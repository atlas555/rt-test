rt-test
=======

concept and test tools for realtime 
include script and tools:
  1.context_switch what used for testing context switch latecny;
  2.interrupt_tool what used for testing interrupt latency;
  3.script what used for loading tools and autoscript.
